import { RespiraClient } from "./RespiraClient";

export const metadata = {
  title: "Respira · Estoicismo Digital",
  description: "Patrones de respiración guiados — calma, foco, energía.",
};

export default function RespiraPage() {
  return <RespiraClient />;
}
